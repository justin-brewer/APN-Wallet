/*
 * SuspenedUserList Messages
 *
 * This contains all the text for the SuspenedUserList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SuspenedUserList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SuspenedUserList component!',
  },
});
