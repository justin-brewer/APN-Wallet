/*
 * DeletedUser Messages
 *
 * This contains all the text for the DeletedUser component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DeletedUser';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DeletedUser component!',
  },
});
