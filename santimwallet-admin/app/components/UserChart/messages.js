import { defineMessages } from 'react-intl';

export const scope = 'app.components.UserChart';

export default defineMessages({
    user: {
        id: `${scope}.user`,
        defaultMessage: 'User',
    },
});
