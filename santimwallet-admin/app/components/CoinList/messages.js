/*
 * CoinList Messages
 *
 * This contains all the text for the CoinList component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.CoinList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CoinList component!',
  },
});
