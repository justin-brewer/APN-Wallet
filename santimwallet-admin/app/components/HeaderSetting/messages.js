/*
 * HeaderSetting Messages
 *
 * This contains all the text for the HeaderSetting component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.HeaderSetting';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the HeaderSetting component!',
  },
});
