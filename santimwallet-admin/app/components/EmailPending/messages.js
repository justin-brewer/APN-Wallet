/*
 * EmailPending Messages
 *
 * This contains all the text for the EmailPending component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.EmailPending';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the EmailPending component!',
  },
});
