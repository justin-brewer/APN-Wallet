/*
 * DeleteUser Messages
 *
 * This contains all the text for the DeleteUser component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.DeleteUser';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the DeleteUser component!',
  },
});
