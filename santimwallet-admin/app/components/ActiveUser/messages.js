import { defineMessages } from 'react-intl';

export const scope = 'app.components.ActiveUser';

export default defineMessages({
    activeUser: {
        id: `${scope}.activeUser`,
        defaultMessage: 'Active User',
    },
});
