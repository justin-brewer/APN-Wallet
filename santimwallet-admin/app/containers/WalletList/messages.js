/*
 * WalletList Messages
 *
 * This contains all the text for the WalletList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WalletList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WalletList container!',
  },
});
