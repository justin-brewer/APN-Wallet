/*
 * GeneralSetting Messages
 *
 * This contains all the text for the GeneralSetting container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.GeneralSetting';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the GeneralSetting container!',
  },
});
