/*
 * CustomPageAdd Messages
 *
 * This contains all the text for the CustomPageAdd container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CustomPageAdd';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomPageAdd container!',
  },
});
