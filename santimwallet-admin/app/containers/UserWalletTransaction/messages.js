/*
 * UserWalletTransaction Messages
 *
 * This contains all the text for the UserWalletTransaction container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserWalletTransaction';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserWalletTransaction container!',
  },
});
