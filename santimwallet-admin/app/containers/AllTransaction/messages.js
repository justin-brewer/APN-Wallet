/*
 * AllTransaction Messages
 *
 * This contains all the text for the AllTransaction container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AllTransaction';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AllTransaction container!',
  },
});
