/*
 * NodeHistory Messages
 *
 * This contains all the text for the NodeHistory container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.NodeHistory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the NodeHistory container!',
  },
});
