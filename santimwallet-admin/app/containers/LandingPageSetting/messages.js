/*
 * LandingPageSetting Messages
 *
 * This contains all the text for the LandingPageSetting container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.LandingPageSetting';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the LandingPageSetting container!',
  },
});
