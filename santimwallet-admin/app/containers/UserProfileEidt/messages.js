/*
 * UserProfileEidt Messages
 *
 * This contains all the text for the UserProfileEidt container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.UserProfileEidt';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the UserProfileEidt container!',
  },
});
