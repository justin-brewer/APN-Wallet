/*
 * ProfileComponent Messages
 *
 * This contains all the text for the ProfileComponent container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.ProfileComponent';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the ProfileComponent container!',
  },
});
