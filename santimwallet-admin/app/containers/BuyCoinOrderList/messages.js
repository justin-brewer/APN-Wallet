/*
 * BuyCoinOrderList Messages
 *
 * This contains all the text for the BuyCoinOrderList container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.BuyCoinOrderList';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the BuyCoinOrderList container!',
  },
});
