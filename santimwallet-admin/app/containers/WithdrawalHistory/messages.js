/*
 * WithdrawalHistory Messages
 *
 * This contains all the text for the WithdrawalHistory container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.WithdrawalHistory';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the WithdrawalHistory container!',
  },
});
