/*
 * AddFaqs Messages
 *
 * This contains all the text for the AddFaqs container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.AddFaqs';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the AddFaqs container!',
  },
});
