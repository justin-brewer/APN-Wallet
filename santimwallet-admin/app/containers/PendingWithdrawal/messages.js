/*
 * PendingWithdrawal Messages
 *
 * This contains all the text for the PendingWithdrawal container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.PendingWithdrawal';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the PendingWithdrawal container!',
  },
});
