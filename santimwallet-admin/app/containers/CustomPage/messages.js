/*
 * CustomPage Messages
 *
 * This contains all the text for the CustomPage container.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.containers.CustomPage';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the CustomPage container!',
  },
});
